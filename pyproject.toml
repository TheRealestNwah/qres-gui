[project]
name = "qres-gui"
version = "1.10.0"  # keep in step with qres_gui.__version__
license = "MIT"
description = "Per-game resolution switching through QRes"
requires-python = ">=3.11"
dependencies = ["PySide6>=6.6", "psutil>=5.9", "pycaw>=20251023"]

[tool.pytest.ini_options]
pythonpath = [".", "tests"]
testpaths = ["tests"]
timeout = 60
